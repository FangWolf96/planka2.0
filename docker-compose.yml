version: "3.8"

services:
  planka:
    image: ghcr.io/fangwolf96/corepoint:latest  # Replace with your actual tag if needed
    restart: on-failure
    volumes:
      - favicons:/app/public/favicons
      - user-avatars:/app/public/user-avatars
      - background-images:/app/public/background-images
      - attachments:/app/private/attachments
    ports:
      - 3000:1337
    environment:
      - BASE_URL=http://localhost:3000
      - DATABASE_URL=postgresql://postgres@postgres/planka
      - SECRET_KEY=notsecretkey

      # 👤 Default Admin (auto-created on first launch)
      - DEFAULT_ADMIN_EMAIL=admin@wolfco.io
      - DEFAULT_ADMIN_PASSWORD=changeme123
      - DEFAULT_ADMIN_NAME=CorePoint Admin
      - DEFAULT_ADMIN_USERNAME=corepoint_admin

      # 📧 SMTP settings
      - SMTP_HOST=ads-ice01.wolfco.io
      - SMTP_PORT=25
      - SMTP_SECURE=true
      - SMTP_USER=crm@wolfco.io
      - SMTP_PASSWORD=uwP7TCtUvgcv
      - SMTP_FROM="CorePoint Admin" <no-reply@armadyne.ca>

    depends_on:
      postgres:
        condition: service_healthy

  postgres:
    image: postgres:16-alpine
    restart: on-failure
    volumes:
      - db-data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=planka
      - POSTGRES_HOST_AUTH_METHOD=trust
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d planka"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  favicons:
  user-avatars:
  background-images:
  attachments:
  db-data:
